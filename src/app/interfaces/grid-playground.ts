import {
  ColSize,
  GuttersSize,
  PaddingSize,
} from '@webblocksapp/ng-bootstrap-components';

export interface GridPlayground {
  sm: ColSize;
  md: ColSize;
  spacing: GuttersSize;
  cardPadding: PaddingSize;
}
